# FinGaze Explorer

Randomly selected illustrative examples, not performance evidence. Seed: 20260907. Three replacement texts sampled from eligible reconstructed targets excluding the previous three examples; two eligible paths per source sampled uniformly without replacement. Human paths have at least two retained fixations; generated paths are nonempty saved attempts. No performance-based selection. All included static data are public and extractable. No backend or weights are published.
